# Docker Environment Variables Template
# Copy this file to .env for local Docker development
# For production, set these in your container orchestration platform

# Node Environment
NODE_ENV=production
PORT=8080

# Database (PostgreSQL)
# For docker-compose: postgresql://kelolaaja:kelolaaja_password@postgres:5432/kelolaaja_db
# For Railway: Use Railway's ${{PGDATABASE.DATABASE_URL}}
DATABASE_URL=postgresql://kelolaaja:kelolaaja_password@postgres:5432/kelolaaja_db

# JWT Secrets (CRITICAL: Generate secure secrets for production!)
# Generate using: openssl rand -base64 64
ACCESS_TOKEN_SECRET=your-access-token-secret-change-this-in-production
REFRESH_TOKEN_SECRET=your-refresh-token-secret-change-this-in-production

# Encryption Key (CRITICAL: Generate secure key for production!)
# Generate using: openssl rand -base64 32
SECRET_KEY=your-secret-key-change-this-in-production

# Frontend URL (for CORS)
WEB_URL=http://localhost:3000

# Database Seeding (Set to 'true' for first deployment only)
# Set RUN_SEED=true in Railway for first deploy, then set to false
RUN_SEED=false

# File Upload (Optional)
# MAX_FILE_SIZE=10485760
# UPLOAD_DIR=./uploads
